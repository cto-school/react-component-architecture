# =============================================================================
# Module 6: React Fundamentals - Git Ignore File
# =============================================================================
# This file tells Git which files/folders to ignore (not track).
# These are files that are either:
#   - Generated automatically (builds, caches, dependencies)
#   - Contain sensitive data (API keys, passwords, secrets)
#   - Specific to your computer (IDE settings, OS files)
# =============================================================================

# -----------------------------------------------------------------------------
# DEPENDENCIES
# -----------------------------------------------------------------------------
# npm/yarn/pnpm download these - each developer installs their own
# NEVER commit node_modules - it can contain 100,000+ files!

node_modules/
.pnp/
.pnp.js
.yarn/
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/sdks
!.yarn/versions

# -----------------------------------------------------------------------------
# BUILD OUTPUTS
# -----------------------------------------------------------------------------
# Generated files from Vite/webpack - rebuild anytime with: npm run build

dist/
build/
.next/
out/
.nuxt/
.output/
.cache/
.parcel-cache/

# -----------------------------------------------------------------------------
# ENVIRONMENT & SECRETS
# -----------------------------------------------------------------------------
# CRITICAL: These contain API keys, secrets - NEVER COMMIT!
# If you accidentally commit a secret, change it immediately!

.env
.env.*
.env.local
.env.development.local
.env.test.local
.env.production.local
!.env.example

# -----------------------------------------------------------------------------
# IDE / EDITOR SETTINGS
# -----------------------------------------------------------------------------
# Personal preferences - each developer has their own setup

# VS Code
.vscode/
*.code-workspace

# JetBrains (WebStorm, IntelliJ)
.idea/
*.iml

# Sublime Text
*.sublime-project
*.sublime-workspace

# Vim
*.swp
*.swo
*.swn
*~

# Emacs
\#*\#
.#*

# Claude Code
.claude/

# -----------------------------------------------------------------------------
# OPERATING SYSTEM FILES
# -----------------------------------------------------------------------------
# OS-generated files that shouldn't be in the repo

# macOS
.DS_Store
.DS_Store?
.AppleDouble
.LSOverride
._*
.Spotlight-V100
.Trashes

# Windows
Thumbs.db
ehthumbs.db
ehthumbs_vista.db
Desktop.ini
$RECYCLE.BIN/
*.lnk

# Linux
*~
.directory
.fuse_hidden*
.Trash-*
.nfs*

# -----------------------------------------------------------------------------
# LOGS & DEBUG FILES
# -----------------------------------------------------------------------------
# Generated during development and debugging

*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# -----------------------------------------------------------------------------
# TESTING & COVERAGE
# -----------------------------------------------------------------------------
# Test outputs and coverage reports

coverage/
.coverage
.nyc_output/
*.lcov
.jest/
test-results/

# -----------------------------------------------------------------------------
# CACHE & TEMPORARY FILES
# -----------------------------------------------------------------------------
# Temporary files that shouldn't be committed

*.tmp
*.temp
*.bak
*.cache
.eslintcache
.stylelintcache
*.tsbuildinfo

# -----------------------------------------------------------------------------
# MISC
# -----------------------------------------------------------------------------

# Local configuration
*.local

# Storybook
storybook-static/

# PWA files
**/sw.*

# Vercel
.vercel/

# TypeScript
*.tsbuildinfo

# Vite
vite.config.js.timestamp-*
vite.config.ts.timestamp-*

# =============================================================================
# TIPS FOR STUDENTS
# =============================================================================
#
# 1. CHECK WHAT'S IGNORED:
#    git status --ignored
#
# 2. REMOVE ALREADY-COMMITTED FILE:
#    git rm --cached filename
#
# 3. IF YOU COMMITTED A SECRET:
#    - Change the password/API key immediately!
#    - The old value is still in Git history
#    - Consider using BFG Repo-Cleaner to remove from history
#
# 4. WHY node_modules/ IS IGNORED:
#    - Can contain 100,000+ files and hundreds of MB
#    - Easily recreated with: npm install
#    - Platform-specific binaries may not work on other machines
#
# 5. WHY dist/ IS IGNORED:
#    - Generated when you run: npm run build
#    - Can be rebuilt anytime from source code
#    - Different on each build (includes hashes)
#
# 6. TO CREATE A GLOBAL GITIGNORE:
#    git config --global core.excludesfile ~/.gitignore_global
#
# 7. USEFUL GIT COMMANDS:
#    git clean -fd        # Remove untracked files (careful!)
#    git clean -fdn       # Dry-run: see what would be removed
#
# =============================================================================
